# Scaling Synthetic Robot Data for Pretraining: What Works for Us (and What Doesn't)

**TL;DR.** Robots don't get trillions of web tokens for free. A robot's "tokens" are joint angles, contacts, camera streams, and physics—so we generate them. We build a lightweight, **Gym-Genesis** pipeline to synthesize large, diverse robot episodes for pretraining vision-language-action (VLA) models like **SmolVLA**. The recipe is simple: **IK expert controllers** inside a **fast, vectorized simulator**, broad **domain randomization**, and outputs that plug straight into imitation learning (IL) and reinforcement learning (RL) evaluation.


## Why synthetic data?

Real robot datasets are expensive and narrow. Synthetic data provides breadth (unbounded variation) and control (perfect labels, calibrated noise). The question isn't "How much can we make?" but "What kind of scale and diversity actually helps pretraining?"

**H1.** Large-scale, task-structured synthetic data generated by IK experts improves VLA pretraining—especially when paired with **strong visual diversity** and **calibrated action noise**.

We don't claim IK solves sim-to-real transfer. It *does* make it feasible for small teams to explore **scale, diversity, and curriculum**—without a warehouse of robots.

## What we build

A **GPU-vectorized data factory** using **Gym-Genesis** to produce high-quality episodes (images, proprioception, actions) with heavy domain randomization (cameras, lighting, object placement). For typical cube-stacking tasks, we generate **≈5k episodes per task**, then use them for **imitation pretraining** and downstream **RL/IL** evaluation. The code path is deliberately minimal and surfaces **native PyTorch tensors**, **LeRobotDataset**-friendly writes, and "batteries-included" examples.

> **Why Genesis?** It's fast, vectorized, and easy to batch. That translates to **throughput** (scale), **stability** (fewer sim stalls), and **repeatability** (clean seeds)—exactly what you want in a data factory.


## The Expert Policy (v2): from Cartesian Waypoints → IK → Joint Interpolation

We use a **batched**, **stage-conditioned** IK controller that outputs smooth 6-DoF joint targets for **SO-101**. Everything runs vectorized in **Gym-Genesis**, so we avoid per-environment Python loops and keep throughput high.

### Setup and notation

* Batch size **B**.
* Joint vector **q ∈ ℝ⁶** (last element **q₆** is the gripper).
* Gripper link frame **L_gripper**; forward kinematics **f_FK(q) ∈ ℝ³**.
* Cube positions from observation: **c₁, c₂ ∈ ℝ^(B×3)**.
* Fixed "tool-down" orientation given by a quaternion from an Euler rotation of **−90°** around **x**:

  **q_tool↓ = Quat(Rₓ(−90°))**


  repeated across the batch.
* Constants that matches the code:

$$
g_{\text{open}} = 0.5, \quad 
g_{\text{closed}} = 0.1, \quad 
z_{\text{off}} = 0.18, \quad 
\delta_z = 0.0981, \quad
\Delta_{xy} =
\begin{bmatrix}
-0.005 \\
\;\;0.02
\end{bmatrix}.
$$

### Stage-conditioned Cartesian targets

Each stage produces a **Cartesian EEF target** $x^\star \in \mathbb{R}^{B\times 3}$ and a **gripper setpoint**:

<p align="center">
  <img src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot-sim/math-eq1.png" alt="Stage-conditioned Cartesian targets" width="70%">
</p>

Gripper values:

<p align="center">
  <img src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot-sim/math-eq2.png" alt="Stage-conditioned Cartesian targets" width="70%">
</p>


The **go_back** stage blends from current joints $q_{\text{start}}$ to a fixed home pose $q_{\text{home}}$ over 10 steps:

<p align="center">
  <img src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot-sim/math-eq3.png" alt="Stage-conditioned Cartesian targets" width="70%">
</p>


and sets $q_{t,6} \gets g_{\text{open}}$.


### Cartesian interpolation (8 waypoints)

Let $x_0 = f_{\text{FK}}(q_0)$ be the current EEF position (batched). We create **8** evenly spaced waypoints:

<p align="center">
  <img src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot-sim/math-eq3.png" alt="Stage-conditioned Cartesian targets" width="50%">
</p>

with orientation fixed to $\mathbf{q}_{\text{tool}\downarrow}$ for all waypoints.

### Warm-started IK per waypoint

We solve IK sequentially with warm starts to preserve continuity:

<p align="center">
  <img src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot-sim/math-eq6.png" alt="Stage-conditioned Cartesian targets" width="50%">
</p>

### Joint-space blending (10 substeps/segment)

Between successive IK solutions $\big(q^{(k)}, q^{(k+1)}\big)$ we insert **10** joint blends:

<p align="center">
  <img src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot-sim/math-eq5.png" alt="Stage-conditioned Cartesian targets" width="50%">
</p>

Concatenating all segments yields the final **list of $(B,6)$** joint targets.

### Gripper trajectories

For grasp, we keep the gripper open for the first $N-5$ steps, then linearly ramp over the last 5:

<p align="center">
  <img src="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/lerobot-sim/math-eq7.png" alt="Stage-conditioned Cartesian targets" width="50%">
</p>

For all other stages, $q_6 \gets g$ is constant across the stage.

### Batch semantics

All quantities are vectorized across $B$: $c_1, c_2, x^{(k)}, q^{(k)}, \tilde{q}^{(k,t)} \in \mathbb{R}^{B\times d}$. The return value is a Python list of length $\approx 8\times10$ (minus one segment), each item a $(B,6)$ tensor.

```python
def expert_policy_v2(robot, obs, stage) -> list[torch.Tensor]:
    """Returns a list of (B, 6) joint targets (SO-101)."""
```

### Why this works (empirically)

* **Continuity:** warm-started IK + dense joint blending suppress configuration jumps.
* **Contact timing:** the late gripper ramp avoids pushing the cube before full alignment.
* **Geometry compensation:** stage offsets $(z_{\text{off}}, \Delta_{xy}, \delta_z)$ account for gripper thickness and cube size.
* **Throughput:** all math is batched; Genesis keeps the sim vectorized and stable.

*Asset tip:* Right under this section, add (i) a tiny parameter box with the constants and (ii) a short GIF showing the 7 stages overlaid with the waypoint path.

---

## Cameras & rendering (diversity without breaking observability)

We use a pinhole model with randomized intrinsics/extrinsics and small **piston jitter** (axial motion). We add mild sensor noise.

Rendering modes:
* **Rasterization** for maximum throughput (default for scale)
* **Ray tracing** when we want speculars/soft shadows to expose brittleness
* **Luisa** when we need programmable BRDFs/special effects

Start modestly (textures, lights, poses), **stabilize grasp success**, then ramp up diversity. Over-randomizing too early hurts success rates.

---

## What helps vs. what doesn't (quick notes)

* **Helps:** Broad camera/lighting domain randomization, small **action noise**, waypoint counts that keep IK feasible, joint-space blending, warm-start IK
* **Hurts (early on):** Aggressive clutter + harsh speculars before policy stabilizes; overly tight lateral offsets; tiny waypoint counts near joint limits

---

## Scope

This repository is a **data generation toolkit**, not a leaderboard or results paper. The goal is simple: make large-scale, randomized robot pretraining data **fast, scalable, and accessible**—so small teams can run big experiments.
